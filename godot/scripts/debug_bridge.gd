extends Node
## DebugBridge — publishes game state to window.__SMALL_WORLD__ on web builds
## and executes queued test commands from window.__SW_QUEUE__.
##
## M0 stub: exists so the autoload wiring resolves. Full JavaScriptBridge
## wiring lands in M0.4 per GODOT_REBUILD_PLAN.md.

func _ready() -> void:
	pass
