extends Node
## Game — top-level orchestration autoload.
##
## M0 stub: exists so the autoload wiring in project.godot resolves and the
## project imports/exports cleanly. Full episode orchestration (director +
## lens + dispatch + timers) lands in M2 per GODOT_REBUILD_PLAN.md.

func _ready() -> void:
	pass
